kind: Service
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    nodePort: 32459
    protocol: TCP
    targetPort: http
  selector:
    app: demo-game-gateway
metadata:
  name: login-gateway
  labels:
    app: demo-game-gateway
  namespace: sandbox
apiVersion: v1
