apiVersion: batch/v1
kind: Job
metadata:
  name: management-admin-bootstrap
  namespace: management
  labels:
    app: management-admin-bootstrap
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 300
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        app: management-admin-bootstrap
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: bootstrap
          image: 183548421506.dkr.ecr.ap-northeast-2.amazonaws.com/kubernetes-ops-service@sha256:132cbc945004812ae3367c21a3408da8f3c9ab1a01b5870ffdea1157c83f5d0a
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "entrypoints.bootstrap_admin"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: management-runtime-secret
                  key: COMMAND_NOTIFY_DATABASE_URL
            - name: MIGRATION_EXPECTED_HEAD
              value: "20260725_0400"
            - name: PROJECT_SLUG
              value: "kubernetes-ops"
            - name: AUTH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: management-admin-bootstrap
                  key: AUTH_PASSWORD
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
